<div class="container">
  <div class="card mb-4 pl-1">
    <div class="card-body d-flex flex-row <%= message&.user&.id == current_user.id ? 'my-message' : 'user-message' %>">
      <div class="col-12">
        <div class="username"><%= message&.user&.username %></div>
        <div class="content"><%= message&.content %></div>
      </div>
      </div>
    </div>
  </div>
